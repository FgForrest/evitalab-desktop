/**
 * Initialize connection remove dialog
 */

import ConnectionRemoveDialog from './ConnectionRemoveDialog.vue'
import { initializeRenderer } from '../../initializer'

initializeRenderer(ConnectionRemoveDialog)
